name: Tensorflow Train - MNIST Saved Model - Master
id: 1cc2e157-0157-487f-8a57-d49ab7b2b032
category: Machine Learning\Train
description: DS tensorflow train which outputs a tensorflow model with spec
inputs:
outputs:
- name: Output Model
  type: ModelFolder
  port: True
  description: The Output model
implementation:
  container:
    conda:
      name: project_environment
      channels:
      - defaults
      dependencies:
      - python=3.6.2
      - pip:
        - pandas
        - tensorflow
        - click
        - PyYAML
        - git+https://github.com/StudioCommunity/CustomModules.git@master#subdirectory=dstest
    command:
    - python
    - -m
    - dstest.tensorflow.saved_model.mnist_saved_model
    args:
    - --export_dir
    - outputPath: Output Model
  invoking:
    module:
    class:
    func:
