name: Keras Train - MNIST Saved Model 2
id: b390f3b5-af6b-4b1f-896f-cb359af74a23
category: Machine Learning\Train
description: DS keras train which outputs a keras model 2 with spec
inputs:
outputs:
- name: Output Model
  type: ModelFolder
  port: True
  description: The Output model
implementation:
  container:
    conda:
      name: project_environment
      channels:
      - defaults
      dependencies:
      - python=3.6.2
      - pip:
        - pandas
        - tensorflow
        - keras
        - click
        - PyYAML
        - git+https://github.com/StudioCommunity/CustomModules.git@migu/NewYamlTest#subdirectory=dstest
    command:
    - python
    - -m
    - dstest.keras.trainer_and_save_model_weights
    args:
    - --model_path
    - outputPath: Output Model
  invoking:
    module:
    class:
    func:
