name: Dummy Train - Master
id: dc9d433a-d0b6-4d97-af5f-9d6e65e8c758
category: Machine Learning\Train
description: DS Dummy Train, deep copy the input structure to output port
inputs:
  - name: Input Model
    type: Dataset
    connectable: true
    description: Inpute Model (will be transfered transparently to output port)
outputs:
- name: Output Model
  type: Model
  connectable: True
  description: The Output model

implementation:
  container:
    conda:
      content:
        name: project_environment
        channels:
        - defaults
        dependencies:
        - python=3.6.2
        - pip:
          - pandas
          - git+https://github.com/StudioCommunity/CustomModules.git@master#subdirectory=dstest
    invoking:
      module: 
      class: 
      func: 
    command:
    - python
    - -m
    - dstest.dummy_train
    args:
    - --input_model_path
    - inputValue: Input Model
    - --out_model_path
    - outputPath: Output Model
